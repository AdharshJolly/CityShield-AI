# TRAINING GUIDELINES
*   All training must occur via scripts located inside `hazards/<domain>/training/`.
*   Output models must be saved to the root `runs/` or `experiments/` directories.
*   Monitor GPU memory and adjust batch sizes accordingly.
