# DATASET MANAGEMENT
*   All massive dataset binaries (images, labels) must remain in the root `data/` or `datasets/` folders to avoid duplication.
*   Do NOT copy images into the `hazards/` directories.
*   Use standard YOLO format (`images/`, `labels/`, `dataset.yaml`).
